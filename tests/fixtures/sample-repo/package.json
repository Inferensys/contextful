{
  "name": "sample-context-repo",
  "version": "0.1.0",
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
